// eslint-disable-next-line no-shadow
enum Status {
	Active = 'active',
	Inactive = 'inactive',
}

const current = Status.Active;
const other = Status.Inactive;

export { Status, current, other };
